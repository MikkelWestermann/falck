{
  "name": "falck-monorepo",
  "private": true,
  "workspaces": [
    "desktop",
    "packages/*"
  ],
  "scripts": {
    "desktop:dev": "cd desktop && bun run tauri dev",
    "desktop:build": "cd desktop && bun run tauri build",
    "install:all": "bun install",
    "clean": "rm -rf node_modules desktop/node_modules bun.lockb desktop/bun.lockb desktop/target desktop/src-tauri/target"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0"
  }
}
